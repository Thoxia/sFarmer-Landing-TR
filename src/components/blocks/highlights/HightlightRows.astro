---
// Highlights Section
// ------------
// Description: A section with a multiple text and image rows.

// Components
// - UI
import TextImage from '../basic/TextImage.astro'

// Content
import highlight01 from '../../../assets/highlights/config.png'
import highlight01mobile from '../../../assets/highlights/highlight-01-mobile.svg'
import highlight02 from '../../../assets/highlights/highlight-02.svg'
import highlight02mobile from '../../../assets/highlights/highlight-02-mobile.svg'
import highlight03 from '../../../assets/highlights/highlight-03.svg'
import highlight03mobile from '../../../assets/highlights/highlight-03-mobile.svg'
import highlight04 from '../../../assets/highlights/highlight-04.svg'
import highlight04mobile from '../../../assets/highlights/highlight-04-mobile.svg'

type HighlightBlock = {
	title: string
	text: string
	image: any
	mobileImage?: any
	imageWidth: number
	imageHeight: number
	imagePosition: 'left' | 'right'
}
const highlightBlocks: HighlightBlock[] = [
	{
		title: '<strong>sFarmer</strong> ile kontrol <strong>sizin elinizde</strong>.',
		text: 'sFarmer, yalnızca oyuncularınızı önemsemekle kalmayıp, sunucu sahiplerini de önemsiyor. Gelişmiş ama basit konfigürasyon dosyaları ile eklenti düzenlemelerinde hiç zorlanmayacaksınız.',
		image: highlight01,
		imageWidth: highlight01.width,
		imageHeight: highlight01.height,
		imagePosition: 'right'
	},
	{
		title: 'İhtiyaç duyacağınızdan <strong>daha fazla</strong> eklenti desteği!',
		text: `sFarmer, yapısı gereği diğer eklentilerle entegre çalışır. 6'dan fazla bölge yönetim eklentisi, 2'den fazla ekonomi eklentisi desteği ve 3'den fazla etkileşim eklentisi ile zaten sunucunuzda olan eklentilerden fazlasına ihtiyaç duymayacaksınız. Desteklenen tüm eklentilerin listesini dökümanlarımızda bulabilirsiniz.`,
		image: highlight02,
		imageWidth: highlight02.width,
		imageHeight: highlight02.height,
		imagePosition: 'left'
	},
	{
		title: '<strong>Bilgili</strong> destek ekibi',
		text: 'THOXIA olarak, en ufak bir şeye ihtiyacınız olduğunda destek ekibimiz yanınızda. Bu eklentiyle ilgili bir soru veya sorun olabilir, veya belki de sadece sohbet etmek istiyorsunuzdur. Kapımız herkese açık! 🙃',
		image: highlight03,
		mobileImage: highlight03mobile,
		imageWidth: highlight03.width,
		imageHeight: highlight03.height,
		imagePosition: 'right'
	}
]
---

{
	highlightBlocks.map((highlight, key) => (
		<TextImage
			id={`highlight-${key}`}
			title={highlight.title}
			text={highlight.text}
			image={highlight.image}
			imageWidth={highlight.imageWidth}
			imageHeight={highlight.imageHeight}
			mobileImage={highlight.mobileImage}
			imagePosition={highlight.imagePosition}
			classes={key === 0 ? '!pb-0' : key === highlightBlocks.length - 1 ? '!pt-0' : '!py-0'}
		/>
	))
}
